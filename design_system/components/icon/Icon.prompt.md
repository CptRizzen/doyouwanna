Inline Lucide icon. Requires the Lucide UMD script on the page; size and stroke are props.

```jsx
<Icon name="map-pin" size={20} />
<Icon name="users" size={18} color="var(--primary)" strokeWidth={2.4} />
```

Use kebab-case Lucide names. Default stroke 2, size 20.
